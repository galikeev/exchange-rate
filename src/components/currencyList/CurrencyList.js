

const CurrencyList = ({NumCode, CharCode, Nominal, Name, Value, Previous}) => {

    return (
        <>
            <div>{NumCode}</div>
            <div>{Name}</div>
        </>
    );
};

export default CurrencyList;